# Series Profiles

This directory defines competition-specific behavior for supported cricket leagues.

Examples include:
- IPL
- PSL
- default fallback profile behavior

Profiles are useful when the same engine needs slightly different assumptions or thresholds by competition without forking the whole runtime.
